{
  "name": "longterm_mongo_queue",
  "version": "1.0.1",
  "description": "A queue implementation for the longterm module, using mongoDB",
  "main": "mongo-queue.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "set NODE_ENV=test && mocha --recursive",
    "cover-test": "set NODE_ENV=test && istanbul cover --report lcov node_modules/mocha/bin/_mocha",
    "coverage-cleanup": "rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ChemicalRocketeer/longterm_mongo_queue.git"
  },
  "keywords": [
    "longterm",
    "queue",
    "mongoDB"
  ],
  "author": "David Aaron Suddjian",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ChemicalRocketeer/longterm_mongo_queue/issues"
  },
  "homepage": "https://github.com/ChemicalRocketeer/longterm_mongo_queue#readme",
  "dependencies": {
    "mongoose": "4.6.0"
  },
  "devDependencies": {
    "codeclimate-test-reporter": "^0.4.0",
    "coveralls": "^2.11.14",
    "istanbul": "^0.4.5",
    "longterm-queue-test": "^1.1.0",
    "mocha": "3.0.2"
  }
}
